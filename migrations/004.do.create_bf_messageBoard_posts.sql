CREATE TABLE IF NOT EXISTS bf_messageBoard_posts(
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    boardId INTEGER REFERENCES bf_messageBoards(id) ON DELETE CASCADE,
    userId INTEGER REFERENCES bf_users(id) ON DELETE CASCADE,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    date TEXT NOT NULL,
    likes INTEGER
)